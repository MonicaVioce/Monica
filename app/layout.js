import './globals.css';

export const metadata = { title: 'Monica — Live Call Console', description: 'Start consented calls and follow the conversation live.' };

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
